# Language-Model-Tokenizer-Explorer
A Streamlit app that demonstrates how text is tokenized into smaller units called tokens, similar to how language models like GPT process text. It shows token count, the list of tokens, and the most frequent tokens, helping users understand NLP tokenization
